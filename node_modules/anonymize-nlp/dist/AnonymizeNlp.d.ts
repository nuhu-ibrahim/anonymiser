import { AnonymizeType } from './common/regexPatterns';
export declare class AnonymizeNlp {
    private maskMaps;
    private readonly typesToAnonymize;
    constructor(typesToAnonymize?: AnonymizeType[], typesToExclude?: AnonymizeType[]);
    anonymize(input: string): string;
    deAnonymize(input: string): string;
    private replaceWithMasks;
    private getTerms;
    private createDocTermsFromTerms;
    private createUniqueAndSortedTerms;
    private processDoc;
    private processTerms;
    private processWithRegex;
    private setEmptyMaskMaps;
    private mask;
}
